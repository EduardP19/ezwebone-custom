# Claude Agents Folder

Use this folder for Claude-only agent briefs and operator notes.

- Runtime app agents live in `agents/`.
- Runtime prompt source for the prequalify agent is:
  `agents/prequalify/prompt.md`
